apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webroot-pvc
  labels: {}
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "1G"
  storageClassName: "nfs"
